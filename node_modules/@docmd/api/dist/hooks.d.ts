/**
 * --------------------------------------------------------------------
 * docmd : the zero-config documentation engine.
 *
 * @package     @docmd/api
 * @website     https://docmd.io
 * @repository  https://github.com/docmd-io/docmd
 * @license     MIT
 * @copyright   Copyright (c) 2025-present docmd.io
 *
 * [docmd-source] - Please do not remove this header.
 * --------------------------------------------------------------------
 */
import type { PluginHooks } from './types.js';
export declare const hooks: PluginHooks;
export declare function getPluginErrors(): {
    plugin: string;
    hook: string;
    message: string;
    filePath?: string;
}[];
export declare function resolvePluginName(key: string): string;
/**
 * Resolve a template reference (from `config.theme.template` or frontmatter
 * `template:`) to its full npm package name. Mirrors `resolvePluginName`
 * but targets the `@docmd/template-*` scope.
 *
 * Accepts:
 *   - `summer`                  → `@docmd/template-summer`
 *   - `template-summer`         → `@docmd/template-summer`
 *   - `@docmd/template-summer`  → `@docmd/template-summer` (unchanged)
 *   - `@scope/template-summer`  → `@scope/template-summer` (unchanged)
 *   - `./relative/path`        → unchanged
 */
export declare function resolveTemplateName(key: string): string;
export declare function loadPlugins(config: any, opts?: {
    resolvePaths?: string[];
}): Promise<PluginHooks>;
